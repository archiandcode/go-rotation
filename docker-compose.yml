services:
  rotation-go:
    build: .
    ports:
      - "8889:8080"
    environment:
      - ADDR=:8080
      - APP_PASSWORD=${APP_PASSWORD}
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
